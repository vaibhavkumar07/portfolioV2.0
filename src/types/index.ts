export interface Project {
  title: string;
  category: string;
  tools: string;
  image: string;
  link: string;
}

export interface CareerItem {
  role: string;
  company: string;
  period: string;
  description: string;
}
